# Backend

Khung thư mục dành cho giai đoạn tiếp theo, theo cấu trúc bản gốc: `src/config`, `src/middlewares`, `src/modules`, `src/utils`.

Chưa sao chép server, kết nối MSSQL, thông tin môi trường hoặc các module nghiệp vụ. Frontend hiện chạy độc lập.
